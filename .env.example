# Production artifact storage. Connect a PRIVATE Vercel Blob store to populate
# these values in Vercel; never commit a real token.
BLOB_STORE_ID=
BLOB_READ_WRITE_TOKEN=

# Set by Vercel at runtime for the connected private Blob store.
# VERCEL_OIDC_TOKEN=
